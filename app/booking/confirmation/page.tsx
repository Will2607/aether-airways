import { BookingConfirmationLoader } from "@/features/booking/components/booking-confirmation-loader";

export const metadata = {
  title: "Booking Confirmed | AetherAirways",
};

export default function ConfirmationPage() {
  return <BookingConfirmationLoader />;
}
