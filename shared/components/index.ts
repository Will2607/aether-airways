/**
 * Composite shared components that combine UI primitives and are
 * used across multiple features (e.g. SearchForm, DateRangePicker).
 *
 * Example: export { DateRangePicker } from "./date-range-picker";
 */
